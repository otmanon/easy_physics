# easy_physics

This repository only hosts a redirect. The site lives at
**https://davicori.github.io/SIGGRAPH-Course-Physics/**

GitHub Pages serves `index.html` (and `404.html` for any sub-path), both of
which forward to that URL. Deployment runs via `.github/workflows/deploy.yml`.
